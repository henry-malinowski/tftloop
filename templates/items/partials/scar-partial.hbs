{{! Content Partial for Scars }}
<div class="header-details">
  <div class="item-description">
    <div class="row-label">{{localize "tftflood.shortDesc"}}:</div>
    <input
      name="system.description"
      type="text"
      value="{{data.system.description}}"
    />
  </div>
</div>